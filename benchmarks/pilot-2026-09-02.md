# Cutover pilot evidence — 2026-09-02

Status: **NOT READY — 1 of 10 minimum paired accepted tasks**

This report records the first offline paired shadow. It is directional evidence,
not a production-cutover decision. The machine-readable records remain outside
the repository at `~/.machinist/pilot/measurements.jsonl` because they contain
host-local operational identifiers.

## Pair 1 — Buzz Workspace issue 730

The historical baseline is [issue 730](https://github.com/Bostonvex/buzz-workspace/issues/730)
and its accepted [pull request 731](https://github.com/Bostonvex/buzz-workspace/pull/731).
Machinist replayed the task from original base
`2be9deabee93cef08517913cc5b2473cf2bdbfdf` in a detached disposable checkout.
The replay could edit only that checkout and was instructed not to commit, push,
change GitHub, deploy, or mutate any external system.

| Measure | Buzz baseline | Machinist shadow | Change |
|---|---:|---:|---:|
| Accepted | yes | yes | no regression |
| Admission to implementation handoff | 1,006.000 s | 670.195 s | 33.4% lower |
| Reported tokens | 3,525,140 | 1,198,197 | 66.0% lower |
| Repair attempts | 0 | 0 | no rework signal |
| Operator touches after admission | 0 | 0 | equal |
| Unattended | yes | yes | equal |

### Measurement boundary

- Buzz admission is the immutable `FACTORY:RUN dispatched` comment at
  `2026-09-01T23:23:05Z`; handoff is pull request creation at
  `2026-09-01T23:39:51Z`. The pull request body records the focused check and
  full suite as green at that handoff.
- Buzz usage is the exact telemetry-correlated Codex implementation turn
  `2fed0782-9a0d-405c-a17d-3ae631279d17`: 62 completed model requests and
  3,525,140 aggregate input plus output tokens. It excludes later reviewer,
  orchestrator, and merge turns, so the baseline token figure is conservative.
- Machinist admission is job creation and handoff is terminal success for job
  `job_7d9c7b3cf073c6cde2770a50`, run
  `run_9f04e74c746b62737e3fcd97`. It used one `dgx-codex` attempt on the Mac mini
  against the DGX Spark OpenAI-compatible endpoint. No API or subscription
  fallback was used.
- `tests/house-rules-gate-card.sh` passed independently after the run. The
  agent-run full macOS suite reported 131 passed, one documented PowerShell
  host skip, and zero failures out of 132. The diff changed only
  `GUIDES/HOUSE_RULES_GATE_CARD.md`.
- The replay also corrected the CRLF line-count prose from 567 to 576, the
  follow-up nit later filed as issue 732. Acceptance is based on correct live
  values and green checks, not byte identity with the historical pull request.

## Gate result

Every evaluable per-task gate passes, but the fail-closed evaluator returns
overall FAIL because one paired accepted task is below the minimum of ten. The
production decision still requires at least 20 representative tasks and two
stable weeks. This task also has a zero-repair baseline, so it provides no
evidence for the 30% repair-reduction target.

The result supports continuing the shadow pilot. It does not support switching
the default execution path yet. The 1.20 million-token candidate total also
shows that compacting Codex startup and repository context remains important,
even when DGX inference removes incremental API cost.
